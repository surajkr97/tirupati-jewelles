# DEBT

Deferred work, each with the phase that should absorb it. MEDIUM/LOW security findings land
here too. Reviewed at Phase 9 §9.8 — nothing CRITICAL may be outstanding at launch.

| ID       | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Raised in                    | Absorb in                                        | Severity                     |
| :------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------- | :----------------------------------------------- | :--------------------------- |
| DEBT-001 | ~~GST treatment of making charges needs the client's CA to confirm.~~ **CLOSED.** The CA has confirmed the treatment MASTER-SPEC §4 specifies: the sale is a composite supply whose principal supply is the jewellery, so making charges are inside the taxable value. `GST_INCLUDES_MAKING_CHARGES` now records a settled position rather than an open question, and the hedging copy is gone from the calculator sheet and the settings screen. Note that it is no longer a one-line change: bills raised under this treatment are printed, sent and legally retained (DEBT-026), so revisiting it means reissuing them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | MASTER-SPEC §4, Phase 5 §5.2 | Phase 8                                          | CLOSED                       |
| DEBT-002 | **Ticker jitter carries consumer-protection exposure.** Displaying a price that differs from the transaction price is exposure under Indian consumer-protection and legal-metrology rules. Mitigations shipped: ±2% clamp, permanent "indicative rate" disclaimer, calculator/bills always use the true rate, working off-switch. Owner should confirm they accept the residual risk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | MASTER-SPEC §8, Phase 4      | Before launch (Phase 9)                          | MEDIUM — business            |
| DEBT-003 | Invoice retention: voided bills are soft-deleted only, never hard-deleted. Indian statutory retention for tax invoices needs confirming so a real purge policy can be written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Phase 8 §8.5                 | Post-launch                                      | MEDIUM                       |
| DEBT-005 | **Dev machine runs Node v23.11.0 — an odd-numbered line that never became LTS and is now end-of-life (no security patches).** It also blocks Prisma 7, whose `engines` require `^20.19 \|\| ^22.12 \|\| >=24.0`. Recommendation: move to **Node 24 LTS** (`nvm install 24 && nvm alias default 24`), then lift the Prisma pin in D-005. CI already runs Node 24, so this is a local-machine gap, not a shipped one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Phase 1                      | Before Phase 9                                   | MEDIUM                       |
| DEBT-006 | **Bottom nav safe-area inset not verified on real hardware.** Phase 2 acceptance criterion 5 asks for a real device or simulator; headless Chromium has no notch, so `env(safe-area-inset-bottom)` resolves to `0px` and the automated test can only confirm the declaration resolves. Needs one manual check on an iPhone or the iOS Simulator that the nav clears the home indicator.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Phase 2                      | Before launch (Phase 9)                          | LOW                          |
| DEBT-007 | ~~SMS provider not wired.~~ **Superseded by D-011** — SMS is deliberately out of scope; see DEBT-011. Original note: `lib/notify/index.ts` ships the `Notifier` interface and a dev console transport; `SmsNotifier.send` throws until MSG91/Twilio credentials exist. Email over SMTP is implemented. Phone OTP therefore cannot be delivered in production yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Phase 3                      | Before launch (Phase 9)                          | HIGH                         |
| DEBT-008 | Common-password blocklist is ~250 curated entries, not a full top-10k list (D-009). Load a real list from a data file before launch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Phase 3                      | Before launch (Phase 9)                          | LOW                          |
| DEBT-009 | `clientIp()` trusts `x-forwarded-for` — **confirmed spoofable, and now narrowed** (SEC-032, Phase 9 §9.1). It takes the _leftmost_ entry, which is the one a client sets; behind a proxy that appends rather than replaces, every request can claim any address. What rides on it: OTP send/verify, login, claim-token, share, enquiry and bill-create limits, plus §9.1's global limit. Per-identifier limits are unaffected, which keeps this MEDIUM. Two halves: (a) code — take the rightmost entry with an explicit trusted-hop count, and fall back to the rightmost _public_ entry when the selected one is private/loopback/link-local, so a wrong hop count cannot put every visitor in one bucket and lock out the site; (b) ops — send a forged header through the real deployment and log what arrives. Keep open until (b) has been run against production. **Phase 9 §9.1 — half (a) is DONE.** `clientIpFromHeaders` reads from the right with an explicit `TRUSTED_PROXY_HOPS`, and never accepts a private, loopback, link-local or CGNAT address as an identity — so a wrong hop count degrades to the rightmost public entry instead of collapsing every visitor into one bucket. Half (b), the ops confirmation, is still owed and is why this stays open. | Phase 3                      | Phase 9 §9.1                                     | MEDIUM                       |
| DEBT-010 | No E2E test for the signup flow: the OTP is only observable in server console output and the harness has no mail catcher. The flow is covered end to end at the integration level. Add a mail catcher (Mailpit) or a test-only OTP retrieval hook so Playwright can drive real signup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Phase 3                      | Phase 9                                          | LOW                          |
| DEBT-011 | ~~The order claim is unreachable — Phase 8's headline feature does not work.~~ **CLOSED.** A single-use claim token is minted with the bill and carried in the §8.4 WhatsApp message, which is delivered TO the number being proven (D-030, D-031). `POST /api/auth/claim` is now the one caller of `claimOrdersForVerifiedPhone`: hashed at rest, single use by conditional UPDATE, 7-day TTL, rate limited per number and per IP, audited, session rotated on success. Proven end to end in a real browser — a walk-in is billed, the customer signs up later, opens the link from the message, and the purchase is in their account with its invoice. `/api/auth/phone/verify` still does not claim, and still leaves `phoneVerified` false: an email code proves the account, not the number.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Phase 3                      | Phase 9                                          | CLOSED                       |
| DEBT-012 | ~~Public rate routes are not rate limited.~~ **CLOSED.** Phase 9 §9.1 added the global per-IP limit in `proxy.ts` (`lib/security/global-limit.ts`) — three tiers, Redis-backed, and it fails **open** so a Redis outage cannot take the site down. Measured: 60 allowed / 15 denied from one address, a different address unaffected, and 100 requests served with Redis stopped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Phase 4 (SEC-009)            | Phase 9 §9.1                                     | CLOSED                       |
| DEBT-013 | ~~The calculator-uses-the-true-rate test cannot run yet.~~ **CLOSED in Phase 5.** Proven twice: `components/calculator/true-rate.test.tsx` mounts the ticker and the calculator in one document with jitter on, asserts the ticker's number moves and the calculator's does not; and `e2e/calculator.spec.ts` does it in a real browser across a real navigation. Both were mutation-checked — leaking a 1% rate nudge into the calculator fails them. Two structural tests also assert `lib/pricing.ts` has zero imports and that no calculator module imports the jitter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Phase 4                      | Phase 5                                          | CLOSED                       |
| DEBT-014 | ~~`revalidateTag('rates')` still matches nothing.~~ **CLOSED in Phase 6.** `RATE_SURFACES` now covers `/`, `/rates` and `/collections`, and `RATE_SURFACE_PATTERNS` adds `/products/[slug]` and `/collections/[slug]` via `revalidatePath(pattern, 'page')` — the form that invalidates every product at once rather than one literal path. Asserted in `lib/rates.cache.test.ts` by iterating the exported constants, so a surface added later without a revalidation entry fails the test. The deeper fix (`cacheComponents` + `cacheTag`) remains a Phase 9 option, not a requirement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Phase 4                      | Phase 6                                          | CLOSED                       |
| DEBT-015 | **Expired calculator shares are hidden but never deleted.** `readShare` refuses anything past `expiresAt`, so the link is dead — but the row stays. Growth is slow and bounded (30-day TTL, 20 shares per IP per hour) and `@@index([expiresAt])` exists so a sweep is a cheap `deleteMany`. `backend/celery_app/` is the eventual home; MASTER-SPEC §2 forbids building on it yet, so this is a Phase 9 cron or a startup sweep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Phase 5 (SEC-011)            | Phase 9                                          | INFO                         |
| DEBT-016 | **Share labels are attacker-controlled text hosted under our domain.** Anyone can create a share and choose the item labels, which render on `/calculator/s/…`. Not XSS — React escapes it and the codebase has no `dangerouslySetInnerHTML` — but a link that looks like it came from the shop is a phishing shape. Mitigated today by `noindex`, an 80-character cap and no branding claim on the page. A real fix is a moderation or ownership model, warranted only if it is ever abused.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Phase 5 (SEC-012)            | Post-launch                                      | INFO                         |
| DEBT-017 | ~~The GST split shown on a bill is not yet CGST/SGST.~~ **CLOSED.** `lib/bills/tax.ts` halves the stored GST rather than applying 1.5% twice, so §8 TEST's "GST split sums exactly to the total GST" holds by construction; the half-rate label is derived from the items' own `gstPct` and omitted when a bill mixes rates. The split is presentation only — **DEBT-001 is untouched**, and the taxable base it divides is still the contested one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Phase 5 §5.2                 | Phase 8                                          | CLOSED                       |
| DEBT-018 | **The buyback and exchange policy pages carry no actual terms.** §6.2 requires the links and they now resolve — they 404'd when first written — but the pages state only what the site can truthfully say: the policy exists and is confirmed in store. Percentages, deductions and exchange windows are the owner's commercial and legal commitments; inventing plausible ones would be fabricating a contract on their behalf. **The owner must supply real copy before launch**; Phase 7's admin panel is the natural home for it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Phase 6 §6.2                 | Before launch (Phase 9)                          | MEDIUM — business            |
| DEBT-019 | **Price filtering and sorting are done in the application over at most 500 candidates.** There is no price column — a price is a function of today's rate (D-020) — so SQL has nothing to sort on. Correct and fast for a catalogue in the low hundreds. `PRICE_SORT_CEILING` bounds it so the trade-off degrades loudly rather than silently; if the catalogue outgrows it, the fix is a rate-derived `pricePaise` column refreshed by the same `setRate` path that busts the caches.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Phase 6 (D-020)              | When the catalogue exceeds ~500                  | LOW                          |
| DEBT-020 | **Lighthouse mobile has not been run.** §6 TEST asks for performance ≥ 90 and CLS < 0.1 on `/products/[slug]`. The CLS-relevant properties are asserted directly in E2E — every image container has a fixed aspect ratio, and the page does not scroll horizontally at 375px — but a real Lighthouse run needs a production build and a throttled profile, and the seeded products have no images, so the number it produced today would not be the number at launch. Run it once real product photography exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Phase 6 §6 TEST              | Phase 9                                          | MEDIUM                       |
| DEBT-021 | ~~Bill download ownership check owed by Phase 8.~~ **CLOSED.** `/bills/{key}` now accepts a valid unexpired HMAC signature, **or** a session that owns the order, **or** an admin session — a correct key on its own returns 404. Verified live: bare key 404, signed URL 200 `application/pdf`, tampered signature 404, expiry extended by an hour 404, admin session 200. `/account/orders/[id]` filters by the session `userId` inside the WHERE clause. See D-028.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Phase 6 (SEC review)         | Phase 8                                          | CLOSED                       |
| DEBT-022 | ~~§7.8 uploads not built.~~ **CLOSED.** The signed-grant flow, the upload control, `confirmUpload`'s independent re-verification and 19 signing tests are in, and `pnpm verify:upload` now passes against the live account: a real PNG accepted (the control), a `.php` and an `.html` renamed `.jpg` both rejected, an SVG rejected as outside the signed allowlist, a 12MB file rejected on the 10MB cap, and tampered parameters rejected with `Invalid Signature`. EXIF is stripped at ingest, not only on delivery.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Phase 7 §7.8                 | Phase 7                                          | CLOSED                       |
| DEBT-023 | **Product list search uses `contains`, not the Phase 6 full-text index.** `/admin/products` filters with a case-insensitive `contains`, which is correct and parameterised but does not rank and will not scale past a few hundred rows. The storefront's `search:` path already has the GIN and trigram indexes; the admin list can reuse them when the catalogue grows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Phase 7 §7.4                 | Post-launch                                      | LOW                          |
| DEBT-024 | **Settings defaults are stored but still not read by the pricing surfaces.** Phase 8 wired `billPrefix` and the shop identity block into the bill, so Settings is no longer write-only. `defaultGstPct` and `defaultMakingPct` are still unread by the storefront, calculator and bill, which use MASTER-SPEC §4's hardcoded 3%. **Now unblocked** — DEBT-001 is closed, so the taxable base is settled and a configurable rate is safe to wire through.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Phase 7 §7.9                 | Phase 9                                          | MEDIUM                       |
| DEBT-025 | **The Cloudinary upload key holds Master Admin, which is more than it needs.** The free tier offers only Master Admin or Media Library User, and the latter cannot upload — Technical Admin, the correct scope, is a paid-plan feature. Tolerable because the secret is server-side only and the upload constraints live in the signature rather than in the role (SEC-020). Move the key to Technical Admin if the account is ever upgraded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Phase 7 (SEC-020)            | If the plan is upgraded                          | LOW                          |
| DEBT-026 | **Invoice retention is enforced by convention, not by the schema.** §8.5 requires soft void and notes the legal retention requirement: Indian GST rules require invoice records to be kept for six years from the due date of the annual return. Today nothing in the application hard-deletes an order — there is no delete action, and `voidBill` only stamps `voidedAt` — but a `deleteMany` in a future sweep, or a `BillPdf` row removed to reclaim space, would breach it silently. Needs an explicit retention policy: a database-level guard or a documented procedure, and a decision on what happens to `BillPdf` bytes older than six years. **Phase 9 §9.1 — now enforced by the database.** SEC-029's least-privilege role has no `DELETE` on `Order`, `OrderItem` or `BillPdf`, so a future sweep cannot breach retention silently; it is refused. What remains is the policy half: a stated retention period, and a decision on `BillPdf` bytes older than six years.                                                                                                                                                                                                                                                                                           | Phase 8 §8.5                 | Phase 9                                          | MEDIUM — legal               |
| DEBT-027 | **A customer name in an Indic script cannot be printed on the invoice.** The PDF is set in a base font encoded in WinAnsi (D-027), so Devanagari, Gujarati and Tamil have no glyphs. `lib/bills/pdf-text.ts` drops what it cannot represent — deliberately, because the alternative is mojibake on a legal document — and a name that reduces to nothing prints as "Customer". Real fix: embed a font with Indic coverage (Noto Sans + Noto Sans Devanagari) and register it with `@react-pdf/renderer`, which also restores `₹`. Costs a committed font binary and a build-time asset path. Matters the first time a customer asks for their name in Hindi.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Phase 8 §8.3                 | Post-launch, or before launch if the client asks | MEDIUM                       |
| DEBT-028 | **Bill PDF bytes live in Postgres (D-026).** Correct today — ~6KB per invoice, under 50MB a year, and the bytes commit with the order — but a database is not an object store. Migrate if an object store is ever configured for anything else, or if invoices start carrying photographs. The move is contained: `storeBillPdf`, `replaceBillPdf` and `readBillPdf` in `lib/bills/storage.ts` are the only three functions that touch the bytes, and `/bills/[key]` reads through them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Phase 8 §8.3                 | If an object store is adopted                    | LOW                          |
| DEBT-029 | **Bill PDF rendering is synchronous in the request.** §8.3 says so explicitly — "Generate synchronously for now (it takes ~1s). Phase 9 moves it to Celery if it becomes a bottleneck" — and it is deliberately outside the `BillSequence` transaction so a render never holds the invoice-number lock. Measured at well under a second for a 3-item bill. `backend/celery_app/` is dormant and waiting. Revisit only if the shop reports Generate feeling slow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Phase 8 §8.3                 | Phase 9, if measured                             | LOW                          |
| DEBT-030 | **The vitest suite and the dev server shared one Redis database.** Fixed in `vitest.setup.ts` (tests now use database 1), but the shape of the bug is worth keeping: `DATABASE_URL` was forced to the test database and `REDIS_URL` was not, so integration tests wrote `rates:current` from the test data and the running dev app served it — `/api/rates` reporting gold 18K at ₹0 on a machine whose Postgres was fine. Any future harness that forces one backing service must force all of them. Phase 9 should assert it: a test that fails if `REDIS_URL` and the development value share a database index.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Phase 8 (TEST)               | Phase 9                                          | LOW                          |
| DEBT-031 | **Database backups now contain customer invoices.** D-026 stores the rendered PDF bytes in Postgres, so a dump that previously held catalogue data and hashed credentials now also holds customer names, phone numbers and purchase histories in a directly readable document format (SEC-023). Nothing is wrong with the storage; the handling requirement changed. Phase 9 deployment must treat backups as personal data — encrypted at rest, access-limited, with a stated retention period that reconciles with DEBT-026's six-year invoice retention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Phase 8 (SEC-023)            | Phase 9                                          | MEDIUM                       |
| DEBT-032 | **Off-scale Tailwind spacing classes are silent no-ops, and 35 remain across Phases 6 and 7.** Phase 2 §2.1 sets `--spacing: initial` and defines only 0/4/8/16/24/32/48/64px, so `gap-3`, `px-3`, `pl-5`, `pl-14` and friends emit NO CSS — `gap` falls back to `normal`, padding to `0`. Verified by probing computed styles, not inferred. Phase 8 fixed the 10 in its own files and the one it had introduced into `components/ui/input.tsx` (where it made `+91` overlap the typed number by 27px). The rest are cosmetic-but-real: elements that read as spaced in the source are not. Two things are needed — a sweep of the remaining files, and a lint rule so the design system fails loudly instead of silently. DESIGN's mandate says to "reject any arbitrary spacing value in a PR"; relying on review has now missed 45 of them.                                                                                                                                                                                                                                                                                                                                                                                                                                | Phase 8 (DESIGN)             | Phase 9                                          | LOW — cosmetic, but systemic |
| DEBT-033 | **A sticky bottom bar makes the bottom nav visible but untappable.** Hit-tested: on `/admin/bills/new` and on Phase 5's `/calculator`, `document.elementFromPoint` at the centre of a nav link returns the `[data-sticky-bar]` div. The bar reserves the nav's height as internal padding and paints its translucent background over it, so the nav shows through dimmed — reading as available while being dead. Not a Phase 8 regression: the behaviour is identical on the storefront screens Phases 5 and 6 signed off, which is why Phase 8 left it alone rather than changing a component four screens depend on. DESIGN to choose: `pointer-events: none` on the bar's lower band so the nav stays reachable, or style the nav as visibly disabled so it stops making a promise it does not keep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Phase 8 (DESIGN)             | Phase 9                                          | LOW                          |
| DEBT-034 | **Every bill is split CGST/SGST; an inter-state sale needs IGST.** `lib/bills/tax.ts` halves the GST unconditionally, which is correct for an intra-state supply and wrong for one crossing a state border — that is IGST at 3%, undivided, on a single line. The shop sells over the counter in one state, so the case does not arise today. It arises the first time they ship to another state, and the fix is not only arithmetic: the invoice needs the customer's place of supply, which nothing currently collects.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Phase 8 (DEBT-001 closure)   | When the shop ships out of state                 | MEDIUM — business            |
| DEBT-035 | ~~The runtime database role is a Postgres superuser~~ (SEC-029). **CLOSED.** The application now connects as `tirupati_app`: `SELECT`/`INSERT`/`UPDATE` only, no `CREATE` on the schema or database, and no `DELETE` on `Order`, `OrderItem` or `BillPdf`. Migrations keep the owner role via the datasource's `directUrl` and `MIGRATE_DATABASE_URL`. The grant set is `scripts/db-roles.sql` — idempotent, so it is reproducible on production rather than a manual change on one machine. Proven by refusal (`CREATE TABLE` and `DELETE FROM "Order"` both denied) and by 120 E2E tests green against the restricted connection. Note the unit suite does **not** validate this: it runs as the owner on `tirupati_test`, because tests `TRUNCATE`, which requires ownership.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Phase 9 §9.1 (SEC-029)       | Phase 9 §9.1                                     | CLOSED                       |
| DEBT-036 | ~~No log redaction, and PII reaches log lines today~~ (SEC-031). **CLOSED.** `lib/log.ts` emits JSON in production and redacts in both environments — emails, phone numbers in every written form, long bearer-shaped tokens, and secret-_named_ keys dropped whole. The measured Prisma case is now a test: the error comes back without the email or the phone and still names the failing call. Routed through it: `serverError`, the rate limiter's fail-closed line, the enquiry handler, `adminAction`. `redact()` is exported for §9.4's Sentry `beforeSend` so the two cannot disagree.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Phase 9 §9.1 (SEC-031)       | Phase 9 §9.1                                     | CLOSED                       |
| DEBT-037 | ~~`parseBillFilters` accepts an impossible date~~ (SEC-033). **CLOSED.** Dates now round-trip through `Date` and are compared against the input, so `9999-99-99` and `2026-02-30` are both refused — the second mattering more quietly, since JS rolls it forward to 2 March and an admin would have seen March's bills with no sign anything was wrong. `page` is bounded too. `lib/bills/query.test.ts` is new (there was no test at all before, which is how it survived Phase 8) and six of its cases fail against the pre-fix parser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Phase 9 §9.1 (SEC-033)       | Phase 9 §9.1                                     | CLOSED                       |
| DEBT-004 | Post-launch backlog, not to be built now: WhatsApp Cloud API auto-send · payment gateway · wishlist / comparison · multi-language (Hindi, Gujarati, Tamil) · gold savings scheme · loyalty programme · PWA offline calculator · rate alert push.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Phase 9                      | Post-launch                                      | —                            |
