/**
 * Loading UI for search. Created by Stage 2 (audit C-2).
 */
import { SearchLoading } from '@/components/shell/route-skeletons';

export default function Loading() {
  return <SearchLoading />;
}
